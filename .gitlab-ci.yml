image: node:10.15-alpine

cache:
  paths:  
  - dist

stages:
    - build
    - publish

build:        
    stage: build    
    script:
        - npm ci

npm_publish:        
    stage: publish
    only:
        - master
    script:        
      - npm i -g --unsafe-perm now
      - now --token=${NOW_TOKEN}
